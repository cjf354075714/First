计算机的编码

一，基本概念
计算机只认识 0 1，如何做到二进制码和字符对应起来，这就是编码的工作
比如，我输入一个 a，那么计算机应该知道这个 a 的二进制码是什么，并且在计算机中就存储这个 a
由于二进制码和字符之间的对应关系有不同，所以就有不同的编码规范：ASCCll、GBK等

1.1 计算机怎么知道这是一个字节的开始，一个字节的结束呢？
首先某种字符编码就有自己的特性，比如一直使用两个字节作为一个字符，并且字节的第一位一定为1
那么计算机在读到第一位为1，就直接读取两个字节，来进行编码转换，下一次照样读取两个字节就行了
这就是计算机读取字节的方式

